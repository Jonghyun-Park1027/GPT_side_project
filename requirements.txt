# ────────── core ──────────
streamlit==1.46.1

# LangChain 0.2 계열 (OpenAI provider 분리)
langchain==0.2.1
langchain-openai==0.1.3
langchain-community==0.2.1
tiktoken==0.9.0          # 토큰 카운트

# ────────── 벡터 스토어 ──────────
faiss-cpu==1.7.4         # CPU wheel

# ────────── HTML/웹 로딩 ──────────
beautifulsoup4==4.12.3   # WebBaseLoader 내부 의존
requests==2.31.0         # 네트워크 호출(명시해두면 안정적)

# ────────── 선택(크롤링 속도 개선·PDF 등 필요시) ──────────
# unstructured==0.18.3
# pydantic==2.7.1         # langchain-core 종속성 해결용(보통 자동 설치됨)
